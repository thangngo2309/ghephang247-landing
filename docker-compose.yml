version: "3.9"

services:
  landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ghephang247-landing
    ports:
      - "3002:3002"
    environment:
      NODE_ENV: production
      PORT: 3002
    restart: unless-stopped
